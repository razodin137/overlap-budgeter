server {
    listen 80;
    server_name _;

    # Health check endpoint (ONCE/kamal-proxy probes /up by default).
    location = /up {
        return 200 "ok";
        add_header Content-Type text/plain;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}